import { Features8 } from "@/components/ui/features-8";

const techStackData = ["Rust", "Stagehand", "Gemini", "Docker"];

export const Features8Demo = () => {
  return <Features8 techStack={techStackData} />;
};
